# T-011: 救済（ヒント・捨てて引き直す）

| 項目 | 値 |
|---|---|
| フェーズ | Phase 1 |
| 優先度 | P0 |
| 依存 | T-008, T-009 |
| 関連 | PRD F5 / 機能設計 3.7,5.1 |

## 目的
最小限の救済2機能を実装する（3層救済は廃止）。

## スコープ（実装する）
- `src/domain/rescue/RescueService.ts`：
  - `useHint(s)`：レベル別コスト消費（やさ無料／ふつう-1／むず利用不可）＋`findHint`
  - `discardAndDraw(s, instanceId, rng)`：手札1枚削除＋コスト消費（やさ0／ふつう-1／むず-2）→補充ガチャ1回（通常ガチャ残は減らさない）

## 受け入れ条件
- [x] ヒントがレベル別の可否・コストに従う（むずは不可）
- [x] 捨てて引き直すで手札が入れ替わり、コストが正しく減る
- [x] 補充ガチャが通常ガチャ残を消費しない
- [x] `rng` は注入

## スコープ外
- UI操作（T-017）

## テスト
- レベル別コスト、手札入替、コスト境界（残不足時の扱い）

## 完了の定義 (DoD)
- [x] ユニットテスト緑、救済が2機能に限定されている
