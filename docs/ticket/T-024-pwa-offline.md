# T-024: 最小SW・PWA（オフライン保証）

| 項目 | 値 |
|---|---|
| フェーズ | Phase 5 |
| 優先度 | P0 |
| 依存 | T-015 |
| 関連 | PRD 非機能(オフライン) / architecture 1.3,9.1 |

## 目的
初回ロード後の完全オフライン動作を、最小SWで保証する。

## スコープ（実装する）
- `vite-plugin-pwa` を有効化し、静的アセット（JS/CSS/辞書JSON）をプリキャッシュ（Cache First）
- `dev-dist/` の gitignore（開発時SW生成物）
- 本番SW（`sw.js`/`workbox-*.js`）は `dist/` 配下に生成

## 受け入れ条件
- [x] 初回ロード後、オフラインでガチャ・合体・記録が動作
- [x] SWプリキャッシュ対象に辞書JSONが含まれる
- [x] ホーム追加/インストールの体験強化は後続フェーズ（本チケットはオフライン保証まで）

## スコープ外
- フルPWA体験（アイコン・スプラッシュ等の作り込み）

## テスト
- E2E：オフライン化して再読込→プレイ可能

## 完了の定義 (DoD)
- [x] オフライン動作を満たす最小SWが有効
