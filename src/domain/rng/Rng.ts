/**
 * RNG（乱数源）インターフェースの公開窓口（機能設計4.6 / 5.2）。
 *
 * 実体の型は `types.ts` に集約している（単一の真実）。rng モジュール配下の実装・
 * 利用側はこの窓口（`./Rng`）から `Rng` を参照することで、リポジトリ構造定義書の
 * 「Rng.ts＝RNGインターフェース」レイアウトを保ちつつ型定義の重複を避ける。
 *
 * `Rng` は `() => number`：呼ぶたびに [0, 1) の数値を返す。実装（決定的 mulberry32 /
 * 非決定的 Math.random ラッパ）はアプリ層で生成して注入する（純粋性・architecture 2.1）。
 */
export type { Rng } from '../types';
